import React from 'react';

const Header = () => {
    return (
        <header>
     <img src="./logo.png" alt=""/>
     <h1>Studio Ghibli API Built Using React</h1>
     <p><em>None of these images belong to me, they are just being used to demonstrate a skill for non-commercial use.</em></p>
        </header>
    );
}

export default Header;